<<<<<<< Updated upstream
name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      NETSCAN_SKIP_MODEL_LOAD: "1"
=======
name: NetScan IDS CI/CD

on:
  push:
    branches: [main, master, branche1]   # ✅ ajout de branche1
  pull_request:
    branches: [main, master, branche1]

env:
  PYTHON_VERSION: "3.10"
  PYTHONPATH: webapp

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install lint dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8

      - name: Check critical Python errors
        run: |
          flake8 webapp/app.py network/ ml/ tests/ --count --select=E9,F63,F7,F82 --show-source --statistics

  tests:
    name: Tests
    runs-on: ubuntu-latest
    needs: lint
>>>>>>> Stashed changes

    steps:
      - name: Checkout
        uses: actions/checkout@v4

<<<<<<< Updated upstream
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
=======
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install project dependencies
>>>>>>> Stashed changes
        run: |
          python -m pip install --upgrade pip
          pip install -r requirement.txt

      - name: Run tests
<<<<<<< Updated upstream
        run: pytest -q
=======
        run: |
          pytest tests/ -v --tb=short

      - name: Run coverage
        run: |
          pytest tests/ --cov=app --cov-report=term-missing

  structure:
    name: Structure
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Check required files
        run: |
          test -f webapp/app.py
          test -f requirement.txt
          test -d webapp/templates
          test -d webapp/static
          test -d network
          test -d ml
          test -f webapp/templates/base.html
          test -f webapp/templates/dashboard.html
          test -f ml/model.pkl
          test -f ml/encoders.pkl

      - name: Install import dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flask pandas scikit-learn joblib

      - name: Check Flask app import
        run: |
          python - <<'PY'
          import os
          import sys
          from unittest.mock import MagicMock

          os.environ["TESTING"] = "1"
          sys.path.insert(0, "webapp")

          sys.modules["scapy"] = MagicMock()
          sys.modules["scapy.all"] = MagicMock()
          sys.modules["network.capture_traffic"] = MagicMock()
          sys.modules["network.simulate_attack"] = MagicMock()
          sys.modules["network.live_ids"] = MagicMock()

          import joblib
          joblib.load = MagicMock(return_value=MagicMock())

          import app as netscan
          print("Flask app imported successfully")
          for rule in netscan.app.url_map.iter_rules():
              print(f"{sorted(rule.methods)} {rule.rule}")
          PY

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [lint, structure]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t netscan-ids:latest .

  summary:
    name: CI/CD Summary
    runs-on: ubuntu-latest
    needs: [lint, tests, structure, docker]
    if: always()

    steps:
      - name: Print summary
        run: |
          echo "Lint:      ${{ needs.lint.result }}"
          echo "Tests:     ${{ needs.tests.result }}"
          echo "Structure: ${{ needs.structure.result }}"
          echo "Docker:    ${{ needs.docker.result }}"

          if [ "${{ needs.lint.result }}" = "success" ] && \
             [ "${{ needs.tests.result }}" = "success" ] && \
             [ "${{ needs.structure.result }}" = "success" ] && \
             [ "${{ needs.docker.result }}" = "success" ]; then
            echo "Pipeline CI/CD succeeded"
          else
            echo "Pipeline CI/CD failed"
            exit 1
          fi
>>>>>>> Stashed changes
